#include<bits/stdc++.h>
#define ll long long int
using namespace std;

int main()
{
    ll a, b, c, flag, i, j, k, l, m, n, x, y, z, num, t;
    cin >> t;
    while(t--)
    {
        cin >> x;
        
    }
    return 0;
}